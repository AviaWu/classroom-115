{
  "name": "classroom-115",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:emulator": "node --test --test-concurrency=1 tests/database-rules.emulator.mjs tests/firebase-store.emulator.mjs tests/database-rules.phase1.emulator.mjs tests/firebase-transaction.emulator.mjs",
    "preview:test": "node tests/preview-server.mjs"
  },
  "devDependencies": {
    "firebase": "12.19.0",
    "jsdom": "26.1.0"
  }
}
